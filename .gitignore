# ── Python ────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
.env
*.egg-info/
dist/
.pytest_cache/

# ── Go ───────────────────────────────────────────────
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work.sum

# ── C++ (CMake) ──────────────────────────────────────
/build/
build/

# ── Rust ─────────────────────────────────────────────
*target/

# ── Common compiled artifacts ────────────────────────
*.o
*.a
*.lib
bench/iops/.run/

# ── IDE / Editor ─────────────────────────────────────
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/kvlang.tmLanguage.json
*.swp
*.swo
*~

# ── OS ───────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Redis runtime artifacts ──────────────────────────
dump.rdb
appendonly.aof

# ── Project-specific ─────────────────────────────────
/tmp/
/post/
/kvlang
/json

# Tool config
.omc/
*.kvfail
*.kv.skip
tutorial/test_failures.csv

/bin/

# ── layout dump 产物（审查 lower 后的 kvspace code）──
*.kv.dump
kvlanglayout

# ── 本地 claude 说明（不入库）──
/.claude/
/claude.md

# ── kvspace shm 测试误落仓库根（相对路径 DSN）──
silent*
dumptest*
d[0-9][0-9][0-9][0-9][0-9][0-9][0-9]

# 五语言对齐 reference 材料（本地查阅用，不入库）
stdlib/kvlang/reference/

# tutorial shm 测试产物（相对路径 DSN）
kvtut*

# tutorial bench 空产物
/tutorial/benchmark.csv

# runtime-rs 测试 shm 产物
kvlangtest*

# shm 测试残留（相对路径 DSN 标记 + slotsboxmalloc 后端文件）
/tut
/wptest
*.sbo.data
*.sbo.head
