// 欢迎加入kvspace世界
lib kvlang/spec/kvspace模型/CABI {
	r####"# C ABI

**符号命名（normative）**：所有 C ABI 符号统一 `<前缀><PascalCase>` 无下划线（即
camelCase）——kvspace 层前缀 `kvspace`（`kvspaceGet` / `kvspaceWriteInPlace`），kvlang runtime 层前缀
`kvlang`（`kvlangRuntimeConnect` / `kvlangLangtypeParse`）。禁止下划线分词（不写 `kvspace_get` /
`kvlang_runtime_connect`），头文件名可用下划线（`kvlang_runtime.h`），导出符号一律无下划线。

访问 kvspace 的唯一 C ABI 由前端 dispatch 静态导出（`kvspace/include/kvspace/kvspace.h`）。读写采用
**借用指针模型**：读返回指向后端常驻空间的指针，调用方不得释放；写返回 body 偏移指针供调用方直接填。
核心符号：

| 符号 | 语义 |
|------|------|
| `kvspaceGet` | 借用读：`*out` 指向后端常驻空间；`resolve=1` 穿透 link；不存在/空值 → `*out=NULL`、返回 0 |
| `kvspaceWriteInPlace` | 就地写：key 已存在、kind 不变、body 长度不变；返回原 box body 偏移指针，违反前置条件即报错，绝不静默重分配 |
| `kvspaceWriteNewPlace` | 新位置写：按 `(langtype, body_len)` 分配新 box、写好 head，返回 body 偏移指针；用于新建或 kind/尺寸变化 |
| `kvspaceListLen` | 返回前缀下直接子项计数 |
| `kvspaceListAt` | 取前缀下第 `idx` 个子项名，写入调用方自备缓冲；配合 `kvspaceListLen` 遍历 |
| `kvspaceDel` | 批量删除给定 key 数组 |
| `kvspaceDelTree` | 删除前缀子树 |
| `kvspaceCp` / `kvspaceCpTree` / `kvspaceCpList` | 复制单值 / 子树 / 直接子项 |
| `kvspaceMkindex` | 建目录，带 `capacity` 预留容量 |
| `kvspaceMkindexExt` / `kvspaceRmindexExt` | 建立 / 撤销 extindex 叠加层 |
| `kvspaceClear` | 清空 |
| `kvspaceWatch` | 一次性等待某 key 变为目标值 |

无句柄的编解码符号（前端静态实现，byte-identical）：`kvspaceTlvEncode` / `kvspaceTlvEncodeMode` /
`kvspaceDecodeHead`，以及构造器 `kvspaceNewPtr`（存目标完整 langtype + 目标 key）、`kvspaceNewChar`
/ `kvspaceNewBool` / `kvspaceNewInt64` / `kvspaceNewFloat64`。

读写各带 `resolve` 参数：`resolve=1` 时透明穿透 ref=1 指针（link）；`kvspaceListLen`/`kvspaceListAt`
另有 `expand_ext` 参数控制是否展开 extindex 子项。head 线格式与 langtype 编码见 [[键系统与数组访
问]]。

**写侧不穿透整键（normative）**：`kvspaceWriteInPlace` / `kvspaceWriteNewPlace` / `kvspaceDel` /
`kvspaceDelTree` 对**最终组件**不解析 link——写/删 Ptr 键只作用该键本体，不重定向到 target
（`Del`/`DelTree` 为 POSIX rm 式：删指针本身，不删被指向者）。解引用由 runtime 显式 `*` 掌控（见
[[ptr]]）。否则「写实参地址 Ptr 到帧槽」会被重定向到 target，导致丢参 / 写坏被指向的值。

**写侧落盘时机（normative）**：`kvspaceWriteInPlace` / `kvspaceWriteNewPlace` 返回 body 偏移指针
给调用方**在返回后**填，故本笔写不可能在返回前落盘——写在下一次 `kvspace*` 调用（读也算：每个 op
开头先落盘上一笔）或 `kvspaceClose` 时落盘；前者失败以返回码 + err 报出，后者失败写 stderr。
进程正常退出（`exit` / main 返回）时，前端（dispatch）对所有未 `Close` 句柄兜底关闭即落盘：
**漏调 Close 不丢数据，真丢也绝不静默**。shm 后端写即落盘（body 指针即最终位置），两后端在
「下一次调用前可见」上一致。

## kvspace C ABI 导出符号（快照）

权威来源是**构建产物**——`nm -D --defined-only /usr/lib/kvspace/libkvspace.so | grep '^kvspace'`（前端
dispatch 静态导出；后端 `kvspace-c` / `kvspace-durable` 各导出同一集合）。下列快照仅作阅读导航，改 ABI 时
不必同步本文：

```
    kvspaceClear
    kvspaceClose
    kvspaceConnect
    kvspaceConst
    kvspaceCp
    kvspaceCpList
    kvspaceCpTree
    kvspaceDecodeHead
    kvspaceDel
    kvspaceDelTree
    kvspaceGet
    kvspaceGetHead
    kvspaceGetPart
    kvspaceListAt
    kvspaceListLen
    kvspaceMkindex
    kvspaceMkindexExt
    kvspaceNewBool
    kvspaceNewChar
    kvspaceNewFloat64
    kvspaceNewInt64
    kvspaceNewPtr
    kvspaceReadReset
    kvspaceRmindexExt
    kvspaceSetPart
    kvspaceTlvEncode
    kvspaceTlvEncodeMode
    kvspaceWatch
    kvspaceWriteInPlace
    kvspaceWriteNewPlace
```
	"#### -> /lib/kvlang/spec/kvspace模型/CABI
}
