// 欢迎加入kvspace世界
lib kvlang/spec/kvspace模型/XValueHead线格式 {
	r####"# XValueHead 线格式

XValue 由 head 与 body 组成。所有后端使用同一线格式：

```
[pow:u8][flags:u8][a:u64 LE][b:u64 LE][langtype UTF-8][zero padding][body]
```

固定前缀为 18 字节。head 长度是 `1 << pow`，body 从该偏移开始。flags 低两位为 storage class；bit 2 为指针标志。未使用的标志位必须为零。langtype 为真实目标类型，不含源码的 `*`/`@` 前缀，后面以零填充至 head 末尾。解码须校验填充、长度和类型。

| class | a | b | body |
|-------|---|---|------|
| 0（短定长） | 0 | 0 | None、标量、目录、map 头等的定长内容；rwfunc 目录值为空 |
| 1（有冗余的长度） | 有效字节数 | 容量，且 ≥ a | 字符/字节数组、代码槽、指针路径；`[0,0]` rwfunc 锚点的 a=5、b≥5 |
| 2（大定长） | 元素数 | 元素宽度 | 张量元素按行主序连续存储 |
| 3（扩展定位） | UTF-8 定位串字节数 | 容量，且 ≥ a | `@ext` 定位串 |

UTF-8 字符数组使用 class 1，`a` 是**字节数**，langtype 里的长度是 Unicode 码点数；两者独立校验。指针使用 class 1 加 flags bit 2，body 是目标 KV 路径；目标类型写入 langtype。

`ro` 与 `vid` 不在 XValue 中。它们存于独立的 KV key：`/.kvspace-meta/` 加原 key UTF-8 字节的小写十六进制编码。读 head 时后端合并这两个元数据字段，复制和删除 key 时同步处理 sidecar。枚举成员按实际物理 key 前缀进行，不存在 index/extindex 成员矩阵或 memindex 载体。

目录 key 以 `/` 结尾，值与不带斜杠的 base key 独立。例如 `/lib/f/` 的 rwfunc 目录值为 class 0、空 body；`/lib/f/[0,0]` 是 class 1、5 字节计数头。map 的 base key 为 class 0、空 body，成员位于 `base·name` 的独立 key。扩展目录叠加通过 class 3 定位串表示。
"#### -> /lib/kvlang/spec/kvspace模型/XValueHead线格式
}
