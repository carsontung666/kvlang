// 欢迎加入kvspace世界
lib kvlang/spec/kvspace模型/指令布局格式 {
	r####"# 指令布局格式

本章界定函数经 layout 后在 `/lib` 下的 KV 布局：指令如何以坐标键落入 kvspace、签名与命名参数如何编
码、调用如何经 extindex 复用指令树、参数如何经指针链解析。

## 空间布局，非线性字节码

传统 VM 编译产线性字节码，调用即压返回地址后跳入口。kvlang 的 layout 不产线性序列，而是**空间布局
**：每条指令展开为一组 `[s0,s1]` 坐标键，可逐槽 `kvspace·get`/`kvspace·list` 寻址，无需反汇编器。函数体永不被
复制——调用时经 extindex 令帧根指向 `/lib` 下同一份指令树（见下 [[调用extindex]] 调用机制）。

## 坐标键 `[s0,s1]`

函数 `/lib/{pkg}·{name}/` 目录下，每个槽以 `[s0,s1]` 坐标键寻址。约定：

- `s0` = 行号：`[0,·]` 为签名行，`[1,·]` 起为指令行（指令 0 落 `[1,·]`）。
- `s1` = 列：`0` 为 opcode 槽，负号 `-j` 为第 j 个读槽，正号 `+j` 为第 j 个写槽。

入口 PC 为 `…/[1,0]`（`layout/src/keytree.rs` 的 `is_entry_pc`：PC 以 `/[1,0]` 结尾即入口）。

### 签名行

函数目录 `/lib/{pkg}·{name}/` 自身是普通**目录节点**：storetype = index、langtype = `index`、**无值**
（body 空）。rwfunc 的**计数头**落签名行锚点 `/lib/{pkg}·{name}/[0,0]`——storetype = index、langtype =
`rwfunc`（裸种类名，**不含签名**），body = `[nr:u16 LE][nw:u16 LE][dynamic:u8]`（读参个数 `nr`、写参
个数 `nw`、是否变参 `dynamic`）。

rwfunc 的各参数类型**不**落 `/[0,x]` 静态槽，而随**参数定义键**承载：`/lib/{pkg}·{name}.[0,-j]`
（读参）/ `.[0,+j]`（写参）——点后缀、与坐标斜杠键 `/[0,±j]` 不同名——langtype `def langtype`，
body = 参数名 + NUL + 该参数的 **def langtype**（匹配类型）串——一个参数定义可匹配多种值的 langtype。参
数顺序由键中的 `±j` 显式记录（命名键不保序），故 func 目录
根**不**落 `/[0,x]` 子槽。

`def rwir` 则相反：`/lib/<op>` 节点自身 langtype = `def rwir`（裸种类名）、body 同计数格式，各参数类型落**位置化**签
名
行 `[0,x]` 子槽（`x<0` 读参、`x>0` 写参，class=1、langtype=`def langtype`，body 为该参数
langtype 串），无 `{param}` 命名键、无实现指令行——跨 runtime 队列按位置收参故须有序签名（见 [[总体方
案]]）。

### 指令行

以 `id_val(x:int64) -> (R:int64) { int64·add(x, 0) -> R }` 为例，`/lib/id_val/` 下：

```
/lib/id_val/  class=0  langtype=rwfunc  body=empty  ← 函数目录
[0,0]         class=1  langtype=rwfunc  body=[nr,nw,dyn]  ← 签名行锚点（s0=0）
.[0,-1]       def langtype:"x\0int64"          ← 参数定义键（点后缀）：参数名 + NUL + def langtype 串
.[0,1]        def langtype:"R\0int64"          ← 参数定义键（写参）
[1,0]         class=1  langtype=rwir  body=counts+"int64·add"  ← 指令0 opcode
[1,-1]        class=1  langtype=rwir    body=counts+"*[0,-1]\0int64"  ← 指令0 读槽1
[1,-2]        class=0  langtype=int64   body=raw(8B)           ← 指令0 读槽2（内联字面量，非 rwir、无 counts）
[1,1]         class=1  langtype=rwir    body=counts+"*[0,1]\0int64"   ← 指令0 写槽1
```

opcode 槽由 layout 按目标写 `rwir` 或 `rwfunc`；两者的 body 均为 5 字节 counts 加目标名。

`/lib` 中的读写槽是共享的布局模板。runtime 首次解码执行帧的 `[s0,±j]` 时，在同坐标写入物理
`*T` XValue，body 为目标 KV key；此后从执行帧读取该指针并回读目标值。内联字面量先落在该帧的
`‥operand[s0,±j]` KV key，再由物理槽指向它。帧内槽覆盖 extindex 的读取结果，不修改共享模板；
opcode `[s0,0]` 仍从 `/lib` 读取。

**`rwir` 值的 body 格式（唯一）**：`counts` = `[nr:u16 LE][nw:u16 LE][dynamic:u8]`（5 字节，指令槽恒
`0,0,0`）+ 槽文本；文本是 opcode 名，或变量引用（相对指针 `*[0,±k]`）。已知目标类型的读写槽
在文本后附 NUL 和类型串；目标已有值时 runtime 以实际 XValue head 的类型为准。**内联字面量不是 `rwir`**——按字
面量自身的 kind 编码（如 `int64` 的 8 字节 raw），**无** `counts`。故读/写槽的值两类：`rwir`（counts +
文本）或内联字面量（如 `int64raw`、`char/utf32` 串）。opcode 槽永不存变量引用，故值拷贝 `=` 与函数调用
`call` 在 KV 层无歧义。
	"#### -> /lib/kvlang/spec/kvspace模型/指令布局格式
}
