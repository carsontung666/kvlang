// 欢迎加入kvspace世界
lib kvlang/spec/kvspace模型/系统变量 {
	r####"# 系统变量

运行时为它管理的对象附带**系统变量**（影子元数据），以 `{对象key}/‥{名}` 形式存放：宿主对象 `/` 下探
一层、键名以 `‥`（U+2025）开头。标识符禁以 `‥` 开头，故所有 `‥` 前缀键均为核心保留，用户代码无法直接
读写——类比 Unix 隐藏文件，默认视图不显示、引擎可见。

系统变量（`X/‥名`，`/` + `‥` 前缀）与用户成员（`X·名`，`·` 中点键）正交，零交集（见 [[键系统与数组访
问]] 键形态三分）。生命周期绑定：`X/‥*` 落在 X 的 `/` 子树内，删除 X 子树即连带清除其全部系统变量。

## vthread 系统变量

宿主 = `/vthread/{vid}`（常量：`runtime/src/keytree.c`、`layout/src/keytree.rs`）：

| 键 | 语义 |
|----|------|
| `‥pc` | 当前执行 PC（绝对路径字符串） |
| `‥status` | 运行状态：`init` / `running` / `wait` / `paused` / `error`；**终态写入的是返回值串**（默认 `"ok"`）并 Notify |
| `‥status/msg` | 状态附带消息；错误时形如 `‥error/msg` 承载错误文本 |
| `‥ctime` | 创建时刻 |

## 帧系统变量

宿主 = 帧根（rwfunc 帧或块帧的根路径）：

| 键 | 语义 |
|----|------|
| *(帧根 extindex)* | rwfunc 帧根经 extindex 指向 `/lib/{pkg}·{name}/` 只读指令树；块帧不建 extindex |
| `‥lib` | rwfunc 帧的 lib 路径，用于识别 rwfunc 帧边界 |
| `‥callpc` | 帧内执行进度（每 op 更新；块帧重入亦更新） |
| `‥returnpc` | 返回地址（帧创建时固化；块帧仅首次设置，重入不覆写） |
| `‥wdst` | **值写参的目标表**（仅当本帧有值写参时落）：每行一个写参的调用方目标 key，行序即写参槽序，地址写参留空行。返回时据此 copy-out |

**值写参的 copy-in / copy-out**（见 [[函数]]）：调用点把调用方该位置的当前值**复制**进帧槽
`[0,+k]`（累加器模式据此拿初值），并把目标 key 记进 `‥wdst`；被调方体内直接读写 `[0,+k]`；返回时
runtime 按 `‥wdst` 把 `[0,+k]` **复制回**目标。地址写参（声明带 `*`/`@`）不走这套——槽里是调用方目
标的地址 Ptr，体内 `*[0,+k]` 直写。

调用点如何写实参 / 结果槽、如何经指针链解析读到重定向路径，见 [[指令布局格式]]。

## 函数标签

具名作用域的标签不落在帧上，而落在函数的 `/lib` 定义处：`/lib/{pkg}·{name}/‥labels/{label}` → 指令序
号（`layout/src/keytree.rs`、`layout/src/code.rs`）。

## 丢弃槽 `_`

写槽写 `_` 即丢弃该位置（见 [[指令架构]]）：它是普通裸名，落帧内 `_` 槽。runtime 内部另为字面量实参建
`._litN` 临时槽（见 [[函数调用]]），与丢弃槽无关、也不属 `‥` 系统变量命名空间。

## `debugger()`

`debugger()` 内建对齐 V8/TypeScript 的 `debugger;` 语句：源码内联暂停点。runtime **不存在**调试模式与
非调试模式之分——`debugger()` **恒**将 `‥status` 置 `paused`，由外部驱动改回 `running` 恢复执行——**暂停
能力就是 `‥status=paused`**（`debugger()` ≡ `vthread·setstatus("paused")`），**不会另设** `‥debugger`
之类的独立系统变量。

## 域与变量之别

注意区分：[[地址空间]] 的结构**域**（`/lib`、`/vthread`、`/networld` 顶层树）与对象随身的 `‥` 系统**
变量**是两种机制。域是路径前缀赋予的固定结构语义，系统变量是任一对象 `/` 子树内的影子元数据。
	"#### -> /lib/kvlang/spec/kvspace模型/系统变量
}
