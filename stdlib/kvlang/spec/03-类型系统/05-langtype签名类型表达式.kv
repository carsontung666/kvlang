// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/langtype签名类型表达式 {
	r####"# langtype 与 def langtype（定义处的匹配类型）

**langtype** 是 XValue head 的语义类型维（见 [[总体方案]]、[[head字段]]），逐字节是一段**类型表达式
串**——base 种类、可选形状 `[dims]`、mapexpr、原型路径等（文法见 [[文法与合法性]]）。它既供人和工具读
写，也落入 kvspace 作运行期匹配依据，贯穿 layout（静态校验）与 runtime（值匹配），是类型的唯一真相。

## langtype 与 def langtype

**langtype 是值的真实类型；def langtype 是定义函数时参数/返回值的匹配类型。** 两者同吃一套文法（附录
[[文法]]），但角色与表达力不同：

| | 出现位置 | 角色 | 表达力 |
|----|---------|------|--------|
| **langtype** | 一个具体 XValue 的 head langtype（见 [[langtype语义类型]]） | 值**是什么**：一个具体实例的类型 | 具体固定——单一种类 + 确定 `[dims]`（每维具体整数），**不含**并集与任何轴量词 |
| **def langtype** | 函数定义处——`def rwir` 的签名行 `[0,x]` 槽、`rwfunc` 的参数定义键 `.[0,±j]`（承载节点的 head langtype 都是 `def langtype`，见 [[storetype_def族与签名行]]） | 参数**可接受的一类值**：**匹配类型** | 完整——并集 `A\|B`、轴量词 `.`/`?`/`*`/`+`、`any`、mapexpr（见 [[匹配语义与变参]]） |

**一个 def langtype 可匹配多种 langtype**——这正是参数多态的来源：`x:int8|int16|int32|int64` 接受四种整数，
`a:[.,*]float32` 接受首维任意、秩 ≥1 的 float32 数组，`v:any` 接受一切。装载与调用时，把**实参值的
langtype** 对参数定义的 **def langtype** 逐值判定：命中即通过，失配报 TypeError（见 [[匹配语义与变参]]）。
参数变参 `...` 是签名层 arity 构造，**不在** def langtype 串内（见 [[函数]]）。

opcode 槽 `[s0,0]` 使用实际的 `rwir` 或 `rwfunc` langtype；layout 在生成时区分已知调用目标，
runtime 再查 `/lib/<opcode>` 分派（见 [[指令槽编码]]）。

## kindexpr（简写）

一条**类型表达式串**——无论写在值处（`langtype`）还是定义处（`def langtype`）——统称 **kindexpr**。故三者
同文法、只差角色：`langtype` 强调「值的真实类型」，`def langtype` 强调「定义处的匹配类型」，`kindexpr` 强
调「一条具体的类型表达式串」。下文与各卷凡书「kindexpr」即指一条类型表达式串。

## 两条 runtime-c ABI（供外部调用）

runtime-c 对外开放两组类型能力，一一对应这两者（`kvlang_runtime.h` 与正典 codec，见 [[cruntime与后端
abi]]），供扩展运行时与嵌入方调用：

- **def langtype 侧（匹配）**：`LangtypeValid` 校验一条 def langtype 串合法、`LangtypeMatch` 把一个值的
  `(kind,ndim,dims)` 对一条 def langtype 作**逐值无状态**匹配。用于签名校验与派发（参数变参判定不在此,
  靠签名行锚点 `[0,0]` 的 `dynamic` 字节,见 [[函数]]）。
- **langtype 侧（解析）**：把一条 langtype 串解析/解码为 `(kind, ndim, dims, 元素字节宽)`——唯一事实源为
  正典 codec `kvspaceDecodeHead`（读 head 的 langtype）与 `kvlangKindexprParse`（layout 侧入口；runtime 侧
  同一能力名为 `kvlangLangtypeParse`，两处 ABI 名待统一，见 [[layout流水线接口]]）。用于零拷贝定位、显
  示、round-trip。

两 ABI 同源同文法（都吃一条 kindexpr 串），差别只在角色：**模式匹配** vs **具体解析**。runtime-c **
不得**为二者各造一份编解码——文法与 codec 只此一处。

## 锚例

- 标量并集、多维形状、动态维、字符串标注：`tutorial/10-types/`（`01-typed-map.kv`~
  `05-tuple-key.kv`）。
- 嵌套 mapexpr 与元组键落入 kvspace round-trip：`tutorial/10-types/04-nested-type.kv`、
  `05-tuple-key.kv`。
- 变参 `...`：`print`/`println`/`min`/`max` 全 tutorial 广泛使用。
"#### -> /lib/kvlang/spec/类型系统/langtype签名类型表达式
}
