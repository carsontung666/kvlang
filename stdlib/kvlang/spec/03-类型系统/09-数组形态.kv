// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/数组形态 {
	r####"# 数组形态

kvlang 的数组有**两种物理形态**：**compact**（稠密：全量元素连续打包进单个 XValue）与
**stringkeymap**（稀疏：散 key，每元素独立落库，未 set 的 key 不存在）。两形态由字面量括号选定，访问
算子不同，但都被 `ndarray·*` 元数据算子统一观测。

## 锚例

- compact 建/读/写/求和：`tutorial/04-ndarray/continuous.kv`。
- 定长初始化（`[]` 全零、`[v...]` 填充）：`tutorial/04-ndarray/fill_init.kv`。
- head 种类与 langtype、reinterpret：`tutorial/04-ndarray/xv_meta.kv`、`xv_reinterpret.kv`。
- numel/dim/shape、xv·at/set：`tutorial/04-ndarray/xv_shape.kv`。
- 多维下标写、reshape：`tutorial/04-ndarray/subscript_write.kv`。
- stringkeymap 字面量与遍历：`tutorial/04-ndarray/sparse_literal.kv`、`string_array.kv`。
- 两形态互转：`tutorial/04-ndarray/separated.kv`。
- 多值坐标段：`tutorial/04-ndarray/geo_coord.kv`。
- 括号规约错误（compact 容变长串）：`error_cases/type_error/compact_string_array.kv`。

## 括号规约

| 字面量 | 形态 | 落库 | 约束 |
|--------|------|------|------|
| `[e0, e1, …]` | compact | 元素连续打包进单个 XValue，langtype 含形状与元素种类（如 `[4]int64`），storetype 为 class 2 | 定长、同种类；元素**不得**为变长（字符串字面量禁入） |
| `{v0, v1, …}` | stringkeymap | 每元素落 `base·[i]` 坐标段独立子 key，容器值 langtype=map langtype（如 `[]char/utf8·int64`），class 0 容器头 body 空，成员按物理 key 前缀枚举 | 变长/可增长；字符串数组等变长集合用此形式 |

- compact 字面量降级为 `array(…)` 构造。变长字符串元素进 compact `[…]` 在 layout 语法检查期报错
  （`compact array [...] cannot hold variable-length string element`）。
- stringkeymap 字面量降级为 `map(…)` 构造，只作赋值右值或 `for-in` 源。

## 定长初始化（无需逐元素赋值）

大数组不可能逐个 `a[i] = v` 赋值。写目标声明**定长维度**（`[N]T` / `[d0,d1]T`，方括号内全为正整数）
时，两种简写把整块 compact 一次成型：

| 字面量 | 语义 |
|--------|------|
| `a:[1024]int32 = []` | 空初始化：按声明维度分配，全零 |
| `b:[256,256]float32 = [1...]` | 填充初始化：按声明维度分配，每元素填 `1`（填充值按元素种类编码，`1`→`float32 1.0`） |

- 两者降级为 `array·fill(langtype[, v])`：`langtype` 即写类型（维度来源），`v` 为填充值（`[]` 省略即
  全零）。runtime 一条指令批量填充，不展开成 N 个字面量参数。
- `[v...]` 只允许**单一**填充值，且写类型必须是定长数组（`[]T` 动态一维、`[.,N]T` 含任意长维均报
  错）。（此处 `[v...]` 是**值字面量**的填充展开，与类型表达式无关。）
- 动态一维 `a:[]int32 = []` 仍是**空数组**（0 长度），不触发填充。
"#### -> /lib/kvlang/spec/类型系统/数组形态
}
