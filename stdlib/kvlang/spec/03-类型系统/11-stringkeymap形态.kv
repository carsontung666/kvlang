// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/stringkeymap形态 {
	r####"# stringkeymap 形态

stringkeymap 数组的容器值是 class 0、空 body 的 XValue（langtype 为完整 map 类型，如 `[int64]·int64`），每个元素在独立的 `base·[i]` key 中；成员由物理 key 前缀枚举：

```kv
nums:[int64]·int64 = {10, 20, 30, 40}    // 每元素落 nums·[0] … nums·[3]
for (x in nums) { … }                    // 遍历散 key 元素
names:[int64]·[]char/utf32 = {"apple", "banana", "cherry"}   // 变长字符串数组
```

容器字面量的写目标**必须带 map langtype**（见 [[map容器]]）——容器值的 langtype 就是它，缺标注
layout 报错；`{}` 尤其无法推断（空字面量不含任何成员）。

散 key 元素以坐标段 `base·[i]` 访问（经 `kvspace·get`），或以 `for-in` / `kvspace·listlen` / `kvspace·listn` 遍历。
坐标段支持多值键（含小数）：`geo·[39.9,116.4] = "Beijing"`，物理以字符串格式落 key（见
`tutorial/04-ndarray/geo_coord.kv`）。

## 两形态互转

| 算子 | 语义 |
|------|------|
| `array·scatter(a)` | compact → stringkeymap（散 key） |
| `array·compact(b)` | stringkeymap → compact |

字面量初始化默认按括号定形态；`scatter`/`compact` 显式转换（见
`tutorial/04-ndarray/separated.kv`）。
"#### -> /lib/kvlang/spec/类型系统/stringkeymap形态
}
