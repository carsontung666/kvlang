// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/ptr {
	r####"# ptr（指针）

**ptr** 不是独立种类。18 字节 head 的 storetype 字节用 bit2 表示指针，低两位用长度类；两个
u64 分别存目标 key 的字节长度与容量，body 存目标 key 路径。langtype 是去掉这一层 `*` 后的目标
类型。源码可写 `*T`，例如 `*int64`、`*Node`；另一层指针由目标 key 的 head 再置指针位表达，
而不是在这一格的 langtype 中重复 `*`（见 [[XValueHead线格式]]）。

指针是**值**：读一个 `*T` 变量得到**地址值本身**（ref=1 Ptr，不解引用），解引用是**显式**的——用一元
前缀 `*`。四项与 C 对齐：

| 形态 | 语义 |
|------|------|
| `&x` | **绝对地址** → ref=1 Ptr（`≡ kvlang·abs(x)`，body = 目标绝对路径） |
| `p`（`*T` 变量读值） | 得地址值本身（不穿透） |
| `*p` | 解引用 → 读 body 目标地址的值 |
| `None` | **空指针**（唯一的空指针字面量；即键不存在=无值） |

`*` 是显式解引用路径。局部 `*p` 沿一条 Ptr 路径读写目标；函数体的 `*[0,±k]` 由 runtime 自动
沿帧槽 `**T` 和实参键 `*T` 两层路径，读写调用方原 key（见 [[函数]]、[[调用extindex]]）。

`[]` 与 `·[…]` 都**不**对指针隐式下标：`p[i]`（`p:*[]T`）报 TypeError——先 `*p` 再下标；`p·[0]` 先按
指针解引用到目标，目标若是 compact 数组则不是容器，同样报 TypeError（见 [[成员访问]]）。

## 空指针即 None

`*T` 的槽只有两种取值：指向某地址的 Ptr，或 **None**。None 在 kvspace 即「无值」——键不存在、读回
None。**不设**「空 Ptr」这第二种空值表示，也不用 `""` 之类字符哨兵冒充空指针（对齐五语言：C `NULL`、
Go `nil`、Python `None`、Rust `None`、TS `null`，无一用空字符串）。

- 字段默认空指针：`left:*TNode=None`；原型不落该键，实例读回 None。
- 判定：`if (p != None) { … }`——`p == None` / `p != None` 是**唯一**的空指针判定写法。
- `p == ""` 是**非法**比较（字符串不是指针），运行期报错。

```kv
struct Node {
	val:int64=0
	next:*Node=None      // 空指针：原型不落该键
}
/n1 = Node{val=1}
/n2 = Node{val=2 next=&/n1}   // next = /n1 的地址（Ptr）
/n2 -> p                // p 为指针变量
*p -> q                 // q = 解引用 p = n2 的实例值
p·val -> v              // 成员访问经指针自动解引用：读 /n1·val
if (p != None) { … }    // 空指针判定
```

`&` 对**裸名** `&x` 产帧槽的绝对地址（`≡ kvlang·abs(x)`）、对**成员链** `&t·2` 产成员路径的绝对地址（`≡
kvlang·abs(t, "2")`），均产 ref=1 Ptr、body 为目标 key 路径。

成员访问 `p·field` 经指针**自动解引用**（对齐 Go/Rust 的 `p.field`）；需要「指针指向的整值」（如指针
指向标量）时用显式 `*p`。二者不冲突：`*p` 取整值、`p·field` 取成员。

函数调用实际使用两层指针：帧参数槽是 `**T`，指向本帧的实参键；实参键是 `*T`，指向调用方
值。只读参和写参都按地址传递，声明中的 `T` / `*T` 描述目标类型，不切换为传值。
"#### -> /lib/kvlang/spec/类型系统/ptr
}
