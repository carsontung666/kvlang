// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/ref存储位置 {
	r####"# 值、指针与扩展定位

wire 的 flags 低两位为 storage class，bit 2 表示指针。普通值按 class 0/1/2 存 body；指针使用 class 1 加指针位，body 是目标 KV key 的 UTF-8 路径，a 为路径字节数、b 为容量。目标 key 若也是指针，沿路径再次读取即可表示多层间接；每格只保存一层。空指针用 None。

`@T` 使用 class 3：langtype 是真实目标类型，body 是扩展定位串，a 为定位串 UTF-8 字节数、b 为容量。核心 runtime-c 不兑现扩展存储；声明支持它的 rwir 按定位串读写实际内容。源码中的 `*`/`@` 类型前缀不进入 wire langtype。

C ABI 的 `kvspaceHead_t.ref` 是从 flags 解码出的兼容视图，不是独立的 wire 字节。`ro`/`vid` 存于独立元数据 key。
"#### -> /lib/kvlang/spec/类型系统/ref存储位置
}
