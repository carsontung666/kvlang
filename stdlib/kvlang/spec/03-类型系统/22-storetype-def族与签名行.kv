// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/storetype_def族与签名行 {
	r####"# storetype——签名行、def 族与目录节点

### 签名不入 langtype 串，而入签名行 `[0,x]` 槽

> **铁律**：rwir/rwfunc 调用槽的 body 以 `[nr][nw][dynamic]` 计数头开头，后接目标名。def rwir 路由头与 rwfunc 的 `[0,0]` 锚点仅有 5 字节计数。参数类型按种类
  分
  两路：**def rwir** 每参独占一个 `[0,x]` 参数轴坐标 key（位置化）；**rwfunc** 不落 `[0,x]`，改由命
  名
  参数键 `{name}` 的 Ptr（其 langtype 即该参类型）承载（按名解析）。

rwfunc / def rwir 的**签名（各参数类型）不写进 langtype 串**，也不塞进计数头节点的 body。计数头只记
计数，参数类型按种类分路承载：

1. **计数头**（两者共有）：`rwfunc` 落签名行锚点 `/lib/pkg·funca/[0,0]`、`def rwir` 落 `/lib/<op>` 节点
   自身，其 body 只记 `[nr:u16 LE][nw:u16 LE][dynamic:u8]`——读参个数 `nr`、写参个数 `nw`、是否变参
   `dynamic`（末位读参 `...`）。**不含任何参数 langtype 串**。rwfunc 的函数目录节点自身是 class 0 空值。
2. **def rwir——位置化 `[0,x]` 签名槽**：每个参数类型落该目录签名行坐标 `[0,x]`——`x<0` 读参、`x>0`
   写参、`[0,0]` 为锚点（见 [[指令布局格式]] 二元坐标 `[s0,s1]`，`s0=0` 即签名行）。每个 `[0,x]` 是
   **`def langtype`** xvalue，body 存该参数的 **def langtype**（匹配类型）串（一参一槽），**纯净、不含变参
   `...`**——
   变
   参是 arity，只落计数头的 `dynamic` 字节。跨 runtime 队列按位置收参，故须有序签名。
3. **rwfunc——参数定义键 `funcDir.[0,±j]` 承载**：func 目录根**不**落 `/` 坐标槽；每个参数随点后缀
   键 `funcDir.[0,-j]`（读参）/ `funcDir.[0,+j]`（写参）承载——langtype `def langtype`，body = 参数
   名 + NUL + 该参数的 **def langtype**（匹配类型）串。点后缀与坐标斜杠键 `/[0,±j]` 不同名，故运行期向帧根
   `frameRoot/[0,±j]` 写实参地址 Ptr 时不会撞上只读静态节点；参数顺序由键中的 `±j` 显式记录（命名
   键不保序），rwfunc 按位置取参、只区分读/写。

于是计数头节点的 langtype 都是裸种类名 `rwfunc`（rwfunc 的 `/lib/pkg·funca/[0,0]`）/ `def rwir`
（def rwir 的 `/lib/<op>` 节点自身）本身，**非「签名 langtype 串」**。def rwir 的签名散在 `[0,x]` 槽、
rwfunc 的散在 `.[0,±j]` 键；实现指令行（若有）在 `[s0>0,·]`。

**符号即读写属性（永久规定）**：参数定义键 `.[op,param]` 中 `param<0` 是**只读参数**（读参）、
`param>0` 是**要写的参**（写参）——函数体内读参一律不得作写槽（含成员/下标/别名路径，见 [[函数]]）。

### def 族：定义节点的 langtype（带空格书写）

「某类型的定义节点」自身的 langtype 用 **`def <base>`** 家族名，**中间保留空格**（读作「`base` 的定
义」）：

| langtype | 落点 | 表示 |
|----------|------|------|
| `def rwir` | `/lib/<op>` | 一条 rwir 的路由头/签名定义（原 `defrwir`，废连写） |
| `def struct` | `/lib/<Name>` | 一个 struct 原型的定义节点（字段类型定义在其子槽） |
| `def langtype` | def rwir 签名行 `[0,x]` 槽 / rwfunc 参数定义键 `.[0,±j]` | 一个参数的类型定义（def rwir body=该参数 langtype 串；rwfunc body=参数名+NUL+langtype 串） |

`def` 与 base 间的空格是**规范强制**：连写形式 `defrwir`/`defstruct`/`deflangtype` 一律废除。

### 目录与签名键

| 路径 | 类型及 class | 成员 |
|------|--------------|------|
| `/lib/` | `lib`，class 0、空 body | 物理 `/` 子 key |
| `/lib/pkg·funca/` | `rwfunc`，class 0、空 body | `[0,0]` 锚点、参数定义键和指令行 |
| `/lib/pkg·funca/[0,0]` | `rwfunc`，class 1、a=5、b≥5 | body 为 5 字节计数头 |
| `/lib/<op>` | `def rwir`，class 0 | body 为 5 字节计数头，签名在独立 key |
| `/vthread/1/` | `rwfunc`，class 3 扩展定位 | 通过 locator 叠加到静态函数目录 |

目录成员由实际 `/` 前缀枚举；map/struct 成员由实际 `·` 前缀枚举。扩展叠加不需要成员矩阵。

"#### -> /lib/kvlang/spec/类型系统/storetype_def族与签名行
}
