// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/langtype语义类型 {
	r####"# langtype（语义类型维，语言视角）

本章界定 **langtype**——一个具体值 head 的**真实类型**（它与定义处匹配类型 `def langtype` 的区别见
[[langtype签名类型表达式]]）。langtype 是完整的类型表达式串，逐字节即类型标注文法，天然含 base 种
类、`[dims]`、map 键值、struct 原型路径。各 langtype 与 storage class 的典型关系：

| langtype（示例） | class | 说明 |
|------------------|-------|------|
| `int64` `bool` `time` `duration` | 0 | 定宽标量 |
| `[N]char/utf8`、`[N]char/utf32`、`[N]char/ascii` | 1 | 字符序列；UTF-8 的 a 为字节数，N 为码点数 |
| `[3,4]float32` | 2 | 定宽张量 |
| `rwir` / `rwfunc` 调用槽 | 1 | 5 字节计数头加目标名 |
| `def rwir` | 0 | 5 字节路由头 |
| `[]char/utf8·int64`、`/lib/Point` | 0 | map/struct 空 body 头，成员为物理 `·` key |
| `def struct`、`lib`、`rwfunc` 目录 | 0 | 空 body 目录值，成员为物理 `/` key |
| `rwfunc` 的 `[0,0]` 锚点、`def langtype` | 1 | 5 字节计数头或类型定义文本 |

指针为 class 1 加指针标志；@ext 使用 class 3，langtype 仍为目标真实类型。

**def 族的来源**（`def` 不是 kvlang 关键字、不在 kv 源码书写）：`def struct`（源码 `struct Name{}`，
layout 落原型定义节点）、`def langtype`（源码参数标注 `x:int64`，layout 铺签名行时逐参生成）由
**layout** 产生；`def rwir`（`/lib/<op>` 路由头）**由各 runtime 注册**——kv 源码不声明 rwir，layout
也不产生 def rwir（见 [[rwfunc布局与def_rwir]]）。

`object`/`scope` **彻底不存在**——无此种类名、无此关键字；异构命名容器一律是 `struct`（见
[[struct]]）。容器**语义类型**只剩 `stringkeymap`、`struct`（口语称「map」指的即 stringkeymap，规范一律书
写种类名 `stringkeymap`）。

同一 langtype 可出现在不同 class；例如 rwfunc 目录值为 class 0，而其签名锚点和调用槽为 class 1。
"#### -> /lib/kvlang/spec/类型系统/langtype语义类型
}
