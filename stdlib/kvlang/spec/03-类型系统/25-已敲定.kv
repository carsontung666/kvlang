// 欢迎加入kvspace世界
lib kvlang/spec/类型系统/已敲定 {
	r####"# 已敲定

- 线格式为 18 字节固定前缀 `[pow][flags][a:u64 LE][b:u64 LE]`，后接 langtype、零填充及 body；head 长度为 `1 << pow`。flags 低两位为 storage class，bit 2 为指针。
- class 0 为短定长值；class 1 为有冗余的长度值（a=有效字节数，b=容量）；class 2 为张量（a=元素数，b=元素宽度）；class 3 为 @ext（a=定位串 UTF-8 字节数，b=容量）。
- langtype 是完整语义类型。源码类型标注的 `*`/`@` 前缀在 wire 中分别表示为指针标志和 class 3，不进入 langtype 串。
- `ro`/`vid` 存于独立 KV key `/.kvspace-meta/` 加原 key 字节的小写十六进制编码，不占 XValue head。
- map/struct 的 base key 存 class 0 空 body，成员直接在 `base·member`；函数和 lib 使用 `/` 目录。成员由物理 key 前缀枚举，无 index/extindex 矩阵或 memindex body。
- rwfunc 目录值为 class 0、body 空；`[0,0]` 锚点为 class 1、a=5、b≥5。def rwir 路由头为 class 0、5 字节。调用槽由 layout 判定为 rwir 或 rwfunc，均使用 class 1，body 为计数头加目标名。
- 字符串与 byte 数组使用 class 1；UTF-8 的 a 是字节数，langtype 的形状记录码点数，解码同时校验。定宽张量使用 class 2。
- 指针 class 1 的 body 是目标 KV 路径；@ext class 3 的 body 是扩展定位串，langtype 为实际目标类型。
- runtime 每次从 kvspace 读取变量和 PC，完成指令后写回；进程内不保留其私有状态副本。
"#### -> /lib/kvlang/spec/类型系统/已敲定
}
