// 欢迎加入kvspace世界
lib kvlang/spec/layout语义/函数 {
	r####"# 函数

rwfunc 使用 `(读参) -> (写参)` 接口，没有返回值。读参在负坐标轴 `[0,-k]`，写参在正坐标轴 `[0,+k]`。参数都须声明类型；读参只读，写参可读写。要修改调用方对象，必须把它列在写参侧。

## 传址模型

函数调用只传地址。调用点的参数指针 `*T` 指向实际 KV 值；被调帧的 `[0,±k]` 再存一个指向该参数指针的指针，因此帧槽为 `**T`。runtime 从帧槽自动解引用一层，交给普通 rwir 的仍是 `*T`；rwir 读取目标 KV 值后运算。函数体中的形参引用由 layout 编为 `*[0,±k]`，最终读写实际目标 key，不复制值本体。

签名中的 `T` 是目标值类型，`*T` 表示目标格自身是一个指针值，`@T` 表示目标格是扩展定位句柄；三者都通过地址绑定。指针深度由沿 KV 路径逐格读取的指针位决定，wire langtype 不含 `*`/`@` 前缀。字面量先物化为 KV 值，再按地址传给函数。普通 rwir 参数按 `*T` 处理；创建 XValue 的初始化算子可以直接接收字面量。

写参直接指向调用方写目标，调用期间读取它能看到调用方当前值，写它立即更新调用方 key。未赋值的写目标读出 None；对 None 做算术是错误。调用期不创建值参副本，也没有返回时 copy-out 或 `‥wdst` 表。

## 只读与类型

读参 `[0,-k]` 不能作为写槽，包括成员、下标和别名写；layout 拒绝这类代码。若要计算新值，写到新局部变量或写参。形参定义的 `def langtype` 可用并集、`any` 等匹配类型；每个参数的定义写在函数目录旁的 `.[0,±k]` key，body 为参数名、NUL、类型串。参数定义键与运行时帧槽是不同的 KV key。

调用时写参个数须匹配；不需要的写参用 `_` 显式丢弃。读参的 `...` 仅表示最后一个参数的变参 arity，不进入 langtype 串。

## 名称与读写轴

同一签名中的参数名不得重复，读参和写参也不得同名。调用点可以将同一个变量同时用于读槽与写槽；两条地址各自解析。

```kv
rwfunc add_one(x:int64) -> (result:int64) {
    x + 1 -> result
}
rwfunc test() -> () {
    3 -> n
    add_one(n) -> n
}
```
"#### -> /lib/kvlang/spec/layout语义/函数
}
