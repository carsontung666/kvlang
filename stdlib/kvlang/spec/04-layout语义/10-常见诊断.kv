// 欢迎加入kvspace世界
lib kvlang/spec/layout语义/常见诊断 {
	r####"# 常见诊断

**error**（拒装载）：

- 参数缺类型标注——`param "x" has no type annotation` / `return value "r" has no type annotation`。
- 读参被写——`read param "A" cannot be used as write slot (read params are read-only)`。
- 值容器参数须标注其实际目标类型；调用统一传地址，见 [[函数]]。
- 参数名重复（读参列表内、写参列表内、读写之间均不得同名，见 [[函数]]）——读写之间
  `param "A" appears in both read-params and write-params`；列表内 `duplicate param "a" in read-params`
  / `in write-params`。**调用**时不受限：`inc(x) -> x` 合法。
- 标注不是种类名——`write "x": unknown type "int"` / `struct field "f": unknown type "intg64"`：
  标量字面量右值处标注须是已知种类名或 `any`，数组字面量右值处须是合法 langtype；**大括号右值
  除外**——那里裸名是 struct 名，存在性归 runtime（见 [[文法与合法性]]）。
- 变参位置非法——`variadic param "x" must be the last read-param` /
  `write-param "r" cannot be variadic`。
- 顶层控制流——`top-level while is not supported — wrap in main()`。
- 写类型与字面量不匹配——`write "x" declared scalar … but assigned an array literal` /
  `declared … but assigned a scalar literal`。
- 裸 rwfunc 遮蔽 builtin——`function "…" shadows builtin`。
- `[]` 下标用于容器——`"[]" 下标不能用于容器 …`。
- `return` 带参数——`return cannot take a value — use write-params for output`。
- 语法错误——`expected …, got …` / `unexpected token … in block` /
  `unexpected token … at top level`。

**warn**（不拒装载）：

- 写槽位置非法 token——`unexpected token … in write slot position`。
- 同行函数调用——`function call … on same line as write slot`。

**info**（提示）：

- 裸顶层 rwfunc——`rwfunc outside lib block — registering under /lib/<name>`。
- lib 名冲突——`package name "lib" expands to /lib/lib/`。
	"#### -> /lib/kvlang/spec/layout语义/常见诊断
}
