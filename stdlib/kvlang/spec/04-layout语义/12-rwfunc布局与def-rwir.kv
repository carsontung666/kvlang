// 欢迎加入kvspace世界
lib kvlang/spec/layout语义/rwfunc布局与def_rwir {
	r####"# rwfunc 布局与 def rwir

## rwfunc 布局细节

`write_func` 往 `/lib/<pkg>·<name>/` 写：

- `[0,0]`——rwfunc 签名锚点，body 仅 `[nr][nw][dynamic]` 计数头；func 目录根**不**落 `[0,±k]` 参数
  类型槽。
- `.src`（ASCII 点后缀，`/lib/<pkg>·<name>.src`）——源码副本，langtype `[n]char/utf8`（class 1）。
- 每个参数定义键——`funcDir.[0,-j]`（读参）/ `funcDir.[0,+j]`（写参），点后缀、与坐标斜杠键
  `/[0,±j]` 不同名；langtype=`def langtype`，body = 参数名 + NUL + 该参的 def langtype（匹配类型）串。此键为签名元数据
  （dump / 类型检查用），不参与运行时形参解析——函数体指令里的形参引用已编译期替换为帧坐标的显式解
  引用 `*[0,-j]`/`*[0,+j]`，运行期沿帧槽 `**T` 与实参键 `*T` 指向调用方值（见 [[调用extindex]]）。
- `[i,j]`（i≥1）——线性指令（见 [[控制流]]）。
- `‥labels/<label>`——标签到 irseq 映射（若有控制流）。

rwfunc 与 def rwir 的签名承载正交：rwfunc 以参数定义键 `.[0,±j]` 承载类型、按位置在帧内解析（不在意参数
**顺序**，只区分读/写参）；def rwir 以位置化 `[0,x]` 静态签名槽承载类型、跨 runtime 队列按位置收参。
故
rwfunc 无 `[0,x]` 槽、def rwir 无 `{name}` 键。

## def rwir 路由头（rwir 统一调度入口）

`/lib/<opcode>`（langtype `def rwir`）由 layout 的声明或 runtime 注册写入：

- **runtime 注册**——各 runtime 启动时经运行时注册接口把自身 myrwircaps 的每个 opcode 写入
  `/lib/<opcode>`；注册前校验各参 langtype。主槽 body `[nr][nw][dynamic]`，各参数类型分别落入
  `[0,x]` 签名行槽（`def langtype` xvalue），无实现指令行。

layout 在生成 opcode 槽时区分 `rwir` 与已知的 `rwfunc`；runtime 仍查 `/lib/<opcode>` 分派。def rwir
路由头仅落于 `/lib/<opcode>`，无 native/扩展/user 的静态归类——每个 runtime 只声明自身 myrwircaps，不
在 layout 层区分。

## native rwir 不落入 kvspace

`+`、`==`、`array`、`sqrt`、`print` 等 native builtin 融合在 runtime 内建表，**不**写 kvspace、运行
时也不回查 `/lib`。数值多类型算子在内建表中融合为单条（`add` 覆盖 int8…float64），派发前剥掉
`<numkind>·` 前缀，输出 kind 由实参决定。

## lib 数据（常量）

`/lib/<name>` 存单个 XValue 常量，以绝对路径读写。`lib` body 内的绝对路径赋值即写常量：

```
lib math {
    /lib/math·Pi = 3.141592653589793
    /lib/math·E  = 2.718281828459045
}
```

任意 kvlang 代码以绝对路径访问 `print(/lib/math·Pi)`。裸名赋值（`Pi = 3.14`）是 `/vthread` 帧局部变
量、跑完回收，**不**入 `/lib`。当前实现**不**内置 stdlib 自举；常量由用户或宿主显式 layout 写入。

## struct 原型

`struct` 声明布进 `/lib/<Name>`：原型定义节点是 class-0 空 body 的 `def struct`；字段类型在
`/lib/<Name>/field`，默认值在 `/lib/<Name>·field`。runtime `struct·new` 以此原型创建实例。
	"#### -> /lib/kvlang/spec/layout语义/rwfunc布局与def_rwir
}
