// 欢迎加入kvspace世界
lib kvlang/spec/runtime语义/执行模型 {
	r####"# 执行模型

runtime 是 kvlang 的**动态语义**阶段：对已由 layout 布进 kvspace 的指令做**纯解释执行**。runtime 不
编译、不优化、不重排；它只按 PC 逐条取指、解码、执行。

runtime 的核心**只有一套**：**C runtime（`kvlang/runtime/`，纯 C + `extern "C"`）是唯一的 runtime 核
心与标准实现**——动态语义以它为准。其余 runtime 一律是**扩展运行时**：嵌入 C runtime 核心、套用其取指
-解码-执行、native 派发与正典 codec，只在其上追加己方 opcode 的解释，绝不另起一套核心或编解码器（见
[[runtime开发规范]]、[[cruntime与后端abi]]）。Rust runtime（`kvlang/runtime-rs/`，二进制名
`kvlang`）即这一架构的**参考实现**——它是 myrwir 宿主，而非第二套等价核心；未来的 **deepx-gpu-compute
（GPU 算子融合计算引擎）与之同架构**：同样嵌入 C runtime 核心，只把 tensor 融合算子作为己方 rwir 追
加。扩展运行时**必须**与 C runtime 核心表现相同的核心动态语义。

## 状态皆在 kvspace

runtime **不得**在 kvspace 之外持有执行状态。PC、栈帧、局部变量、调用链一律落 kvspace 的 KV 树；进程
可在任意指令边界退出，其执行状态完整留在 kvspace，另起进程按同一 PC 续跑而语义不变（崩溃恢复）。

- **PC 是一个 KV 路径字符串**，形如 `/vthread/<vid>/[s0,s1]`（含嵌套帧时逐段延长，见 §四）。PC 既是
  取指地址，也是当前帧的定位符。
- **读写**：runtime 经 `kvspaceGet` 借用读值，借用在下一次读写前失效；写值经 `kvspaceSetValue`
  同步提交完整 XValue。PC 和变量每条指令都从 kvspace 读回，不保留私有事实副本。

## 坐标系

一条指令在其所属函数子树内由二元坐标 `[s0,s1]` 定位：`s0` 为指令行（行 0 为签名，指令从行 1 起），
`s1` 为参数轴（0 为 opcode，负数为读参绑定 `[0,-k]`，正数为写参绑定 `[0,k]`）。签名槽 `[0,0]` 为
opcode/签名行锚点；计数头落该锚点——rwfunc 的计数头在 `/lib/<pkg>·<name>/[0,0]`（langtype `rwfunc`），
rwir 路由头的在 `/lib/<op>` 节点自身（langtype `def rwir`，见 [[runtime开发规范]]）。

## 指令解释循环

runtime 对一个 vthread 的执行是如下循环（C 实现 `runtime/src/kvcpu.c:kvlangKvcpuExecuteMode`）：

1. **状态门**：读该 vthread 的 `‥status`。仅当状态为 `init`、`running`、`wait` 之一时**继续**；其余
   状态即**停机**——`paused` 为调试暂停、`error` 为出错，其余为**终态返回值串**（默认 `"ok"`），进程可
   正常退出。
2. **深度检查**：帧深超过实现上限（`MAX_STACK_DEPTH`）时置 `RecursionError` 并停该 vthread。
3. **取指解码**：由 PC 定帧根、经帧的 `‥lib` 软链定位共享指令树。`RwirDecode` 首次读取读写槽时
   将模板描述实体化为执行帧同坐标的 `*T`，之后读取物理槽并回读目标 KV 值；稠密探查遇空即停。
   opcode 仍从共享指令树读取。空 opcode 视为 `/lib` 损坏或跳转越界，报 `RuntimeError` 停该 vthread。
4. **派发执行**（见 §五）。
5. **推进 PC**：多数指令执行后由自身写回新 PC（`NextPc` 求后继，或 call/return/br/goto 改写 PC）；循
   环回到 1。

## 派发链

每条指令的归属互斥。decode 期从当前 KV 指令槽计算派发码 `op_id`，执行期据此查静态能力表：

1. **myrwircaps 内的核心算子**（`op_id ≥ 0`，直查静态能力表，枚举见 [[runtimec内建]]）——本 runtime
   就地兑现。
2. **控制指令**（`op_id ∈ {OPID_CALL/RETURN/BR/GOTO}`）——`call` / `return` / `br` / `goto`。
3. **`=` 拷贝**（`op_id == OPID_COPY`）——值拷贝语义。
4. 以上皆否（`op_id == OPID_notinmyrwircaps`，即 `notinmycaps`）时查 `/lib`：**`notinmyrwircaps`**——
   `/lib/<opcode>` 存在 def rwir 路由头（kind=`def rwir`）→ 路由到能兑现它的其它 runtime（见
   [[runtime开发规范]]）。
5. **否则视为用户 rwfunc 调用**——opcode 移入 `reads[0]`，改写为 `call` 指令。

`op_id` 每条指令重新计算，控制/拷贝/myrwircaps 成员全折入同一整数字段，热路径无 opcode 字符串匹
配。myrwircaps 判定先于 `/lib` 查表，故与核心算子同名的 `/lib/<op>` 不改变派发。

## 调用即无返回值——输出走写参映射

kvlang 的调用**没有返回值表达式**。函数/rwir 的输出一律经**写参映射** `f(a) -> r` 显式落到调用者指定
的写槽；一条指令可有零个或多个写参。

- **call**：创建子帧 `/vthread/<vid>/<父帧路径>/[s0,0]/`，把实参经 Ptr 链绑定进子帧的读参槽
  `[0,-k]`、写参槽 `[0,k]`（零拷贝跨帧传参，不复制实参内容），子帧记 `‥callpc`（发起调用的 PC）与
  `‥returnpc`（返回后继续的 PC），PC 进入子帧首指令。
- **return**：先把父 PC 或终态写入 kvspace，再清理子帧；顶层帧返回使整个 vthread 结束。
  返回链断裂（帧无 `‥returnpc`）**必须**报 `RuntimeError` 并保留该帧供排查。
- 读参**只读**、写参**可写可读**；修改型指令**必须**把结果显式写入其写槽（见 [[函数调用]]）。
	"#### -> /lib/kvlang/spec/runtime语义/执行模型
}
