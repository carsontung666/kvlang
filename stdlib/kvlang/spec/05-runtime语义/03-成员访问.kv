// 欢迎加入kvspace世界
lib kvlang/spec/runtime语义/成员访问 {
	r####"# 成员访问（运行期语义）

本章界定运行期对复合数据的访问语义：成员符 `·` 的解引用规则、动态键、指针的单跳类型检查、容器
物理成员键的运行期行为、compact 数组下标。类型的静态形态（种类、langtype、数组形态）见类型系统卷；本
章只讲 runtime **如何在 kvspace 中定位与读写**这些结构。

## 两个索引符号

kvlang 用两个符号访问复合数据，语义严格分离，runtime **不得**混用：

| 符号 | 目标 | 说明 |
|------|------|------|
| `a[i]` | **数组下标** | compact 数组的元素访问，`i` 为整数 |
| `base·name` | **容器成员 / 限定名** | `·`（U+00B7，中点）是成员访问符 |

容器成员一律走 `·`（或 `kvspace·get`/`kvspace·set`）；`[]` 只索引 compact 数组。

## 容器值与物理成员键

复合命名数据（`stringkeymap`、`struct`）的 `base` 键只存本格的 class-0 空 body 值。
成员 `base·name` 是独立物理 key；kvspace 按 `base·` 前缀枚举成员名，不维护 memindex 矩阵。

- **写成员**：`rec·name = 值` 的写槽是完整成员键 `rec·name`，`=` 为值拷贝。
- **读成员**：`rec·name -> v` 直接取 key `rec·name`；缺失成员的动态读（`kvspace·get`）返回 `None`。

## `·` 的解引用：按指针优先，按名回退

`base·name`（读写同规则）中 `base` 的解析在 runtime 按以下顺序确定成员键：

1. **按指针解引用**：`base` 的 head 指针位为 1 时，成员键 =
   `target(base)·name`。例：`&n1 -> p` 后 `p·val` 解析为 `/…/n1·val`。
2. **按名回退**：`base` 非 Ptr（局部 struct 实例等）时，成员键 = `解析(base)·name`；`解析()` 帧感知
   ——裸名 → `帧根/base`，`/` 开头 → 直通绝对路径。

该规则让"局部结构体"与"指针成员访问"共用一套语法：结构体的 `base` 非 Ptr（走按名），指针变量持 Ptr（按
其 target）。跨函数共享的数据**应**放绝对路径（`/n1`），使不同帧的指针都解引用到同一 key。

3. **base 形态判定**：解析出的 `base`（Ptr 则取其**目标**）**必须**是容器——struct 实例（kindexpr 以
   `/` 开头）或 typed map；或**路径串**（`[]char/*`，显式形式 `kvspace·get(path,
   k)` 的 base 就是它，其值即成员键的父路径）。compact 数组与标量两者都不是，报 TypeError（`·` 要求容
   器；compact 数组用 `[]` 索引）；None 是动态读，返回 None。**不得**把非容器、非路径串的 body 字节当路
   径串拼成员键——那会静默给出脏值或 None。

## 动态成员名 `·*`：解引用操作数取 memitemkey

`base·*k` 用 `k` 的**值**作为成员键：`·*` 令 runtime-c **解引用/求值**操作数 `k`，以其值定位
`base·<k 值>`（等价显式形式 `kvspace·get(base, k)` / `kvspace·set(base, k, v)`）。裸 `base·k`（无 `*`）用字面
名 `k`；`base·*k` 才取 `k` 的值。

**指针作 memitemkey 必须 `*` 开头**：当作为 key 的操作数是**指针**（ref=1 Ptr）时，**必须**写
`base·*k`，由 runtime-c 沿 Ptr 解引用到被指向值、再以该值作 memitemkey；对指针操作数省略 `*` 非法（不
会自动解引用）。解引用得到的值随后**逐字**作键——`*` 是**显式** deref，与逐字铁律（[[文法与合法性]]，
禁止**自动**改键）不冲突：改写由源码 `*` 显式授权，非 runtime 擅自所为。

## 指针：完整 langtype + 单跳类型检查

指针（`Ptr`，head ref==1，源码前缀 `*`）在 kvspace 中存**目标的完整 langtype + 目标 key**
（`kvspaceNewPtr` 是唯一构造入口，runtime **不得**自造 Ptr 编码）。

- **单跳类型检查**：写入指针时，实现**必须**校验指针声明的目标 langtype 与被指向 key 的实际 langtype
  一致——只查一跳，不递归展开链。
- **读解引用链**：读参解析（`resolve_read_path`）从命名 Ptr 出发，只追显式 `Ptr`（ref==1）链到非指针
  值即停；**不得**把 char 字符串值误当路径继续追（否则字符串实参内容会被当键，穿两层调用即变
  `None`）。写侧解析（`ResolveWriteSlot`）遵循同一纪律。

## 扩展世界句柄（class 3，@ext）

`@ext` 使用 storage class=3，不使用独立 `ref` 字节。head 的 langtype 是目标类型（不带 `@` 前缀），
两个 u64 分别记录定位串字节长度与容量；body 是 UTF-8 **定位串**（如
`/networld/<host>/proc/<pid>/stdout`），不是真实值字节。读取该 key 时
由支持 `@` 的 rwir（如 runtime-rs 的 fs/networld 一族）按 body 定位串还原真实字节（见 [[runtime开发规
范]]、[[执行模型]] §八）。真
实 xvalue 落在扩展世界（fs 文件、gpu tensordata 等），非 kvspace 元存。

## compact 数组下标

数组有两种存储形态：**compact**（`[...]` 字面量，元素连续打包进单个 XValue，head kind = 元素类型）与
**stringkeymap**（`{...}` 字面量，散 key，每元素落 `base·[i]` 坐标段独立子 key）。下标 `a[i]` / `xv·at` /
`xv·set` 用于 **compact** 形态；stringkeymap 形态用 `for-in` / `kvspace·get` 遍历访问。

下标的操作数**必须**是 compact 数组**值**本身。Ptr（ref=1）是寻址用的间接层，不是数组：`p[i]`（`p:*[]T`）
报 TypeError——先显式解引用 `*p` 再下标（`*` 是唯一解引用路径，`[]` 不做隐式解引用，见 [[ptr]]）。

- **下标读**：`a[i] -> e`（layout 脱糖为 `xv·at`/`kvspace·get`），按 `kvlangXvalueElemSize(kind)` 定的元
  素字节宽零拷贝定位第 `i` 个元素。
- **下标写**：`a[i] = v` / `v -> a[i]`（两种书写等价，脱糖为 `xv·set`，见 [[指令架构]]），就地改第 `i`
  个元素；索引个数须等于维数，越界报 IndexError。
- `xv·at(a, i) -> e` / `xv·set(a, i, v) -> a` 是显式等价形式。

元素字节宽由 kind 决定（`char/utf8`=1，`int32`/`float32`=4，`int64`/`float64`=8……），数组与字符串的
元素访问据此零拷贝定位。锚例：`tutorial/04-ndarray/`（`continuous.kv`、`subscript_write.kv`、
`separated.kv`、`string_array.kv`）、`tutorial/05-dict/`、`tutorial/12-struct/`。
	"#### -> /lib/kvlang/spec/runtime语义/成员访问
}
