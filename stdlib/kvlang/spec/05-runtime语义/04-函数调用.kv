// 欢迎加入kvspace世界
lib kvlang/spec/runtime语义/函数调用 {
	r####"# 函数调用

调用 `/lib/<pkg>·<name>` 下的 rwfunc。源码可以写绝对路径、`pkg·name`，或同包优先的裸名。layout 根据已知目标将 opcode 槽写成 `rwfunc`；runtime 从 KVSpace 读取函数的 `[0,0]` 锚点及参数定义。

调用时在 `/vthread/<vid>/[depth]` 创建子帧，使用 class 3 扩展定位叠加函数目录的只读指令树。帧内 `‥lib`、`‥callpc`、`‥returnpc` 都写在 KVSpace；PC 也只存于 KVSpace。每条指令边界重新读取 PC 和状态。

## 参数绑定

调用点把每个实参的实际 KV 路径写为 `*T` 参数指针；字面量先落临时 KV key。子帧 `[0,±k]` 存指向该参数指针的第二层指针 `**T`。被调函数体的形参引用是 `*[0,±k]`，runtime 先从帧槽读参数指针，再由普通 rwir 读取目标值。写参沿同一链直达调用方目标，返回时无需拷贝。读参只读，由 layout 验证。

参数签名在函数旁的 `.[0,±k]` key，类型为 `def langtype`，body 为参数名、NUL、匹配类型串。它们不覆盖帧内 `/[0,±k]` 绑定键。`[0,0]` 锚点单独存 5 字节读参、写参和变参计数头。普通 rwir 的路由头在 `/lib/<opcode>`，签名在它的 `[0,x]` 子 key。

## 调度

runtime 先按 myrwircaps 兑现本地 rwir；不在能力集内而有 `def rwir` 路由头的指令交给对应外部 runtime；其余已知函数按 rwfunc 调用。外部 rwir 也从 KVSpace 读写 PC 与参数，不依赖进程私有的执行状态。
"#### -> /lib/kvlang/spec/runtime语义/函数调用
}
