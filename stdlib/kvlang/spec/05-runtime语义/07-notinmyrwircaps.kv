// 欢迎加入kvspace世界
lib kvlang/spec/runtime语义/notinmyrwircaps {
	r####"# myrwir 扩展运行时（handoff 与就地）

## handoff 协议（跨进程扩展）

跨进程扩展（如 Go json、Py numpy、op-gpu）经共享队列 handoff：

```
/lib/<opcode>/vids/<vtid> = pc   # 中央 runtime 写，扩展消费（strkeymap 队列）
```

第一个 rwir 的 `vids` 是真实 strkeymap；后续同 opcode 的 `vids` 是 `Ptr` 指向第一个（共享同一队
列）。时序：

1. 中央 runtime `notinmyrwircaps` 命中 → `handoff_external_rwir`：`Set /lib/<op>/vids/<vtid>=pc`。
2. 扩展发现队列中的 vtid，`RunSeq(pc)`：从 PC 起逐条解码，只要 `opcode ∈ 本扩展.myrwircaps` 就执行，
   遇第一条不在 myrwircaps 内的指令即停（**顺序执行，非并行 batch**）；写回最终 PC，删 `vids` 项。
3. 中央 runtime `Watch` 直到 vtid 从队列消失（30s 超时），继续。

中央 runtime **不推进 PC**——扩展批量执行后已写回最终 PC，中央直接读新 PC 续跑。

## 就地扩展（同进程）

同进程扩展在驱动循环里就地派发（Rust `run_vid`：`rwir::is_inproc(op)`——即 `op ∈ myrwircaps`——命中 →
`rwir::dispatch`），无 handoff、无 watch。runtime-rs 的 myrwircaps（就地集合）当前登记：

```
term      print / println / cerr / input
json      json·to / json·from
http      http·call
kvlang    kvlang·vet / ·format / ·layout / ·dump
networld  networld/proc·exec
          networld/fs·size / ·read / ·write / ·append / ·list / ·del / ·mkdir / ·exists
```

就地与跨进程只是**执行位置**之别，职责线一致——两者都只经 `kvlangRwirext*` 解析参数、只经 `kvspace*`
正典 ABI 读写编解码。

## 参数解析与零拷贝读

扩展**只经** `kvlangRwirext*` 宿主 ABI 拿参数，**不得**自行解析 rwir 槽位：

- `Params(pc)`：返回 opcode + 读/写参名（`\n` 分隔）。
- `ResolveRead(pc, idx)`：读参解析为**字符串**（stringkeymap 值展开为 `[e0, e1, …]`）；**对数组只返
  首元素**。
- `ResolveReadPath(pc, idx)`：读参解析为**KV 路径**（变量/临时 → 帧槽路径；绝对路径 → 直接返回；内联
  字面量 → 空串）——供**零拷贝读整块 ndarray**：拿到容器路径后经 `kvspaceGet` 取 XValue、`DecodeHead` 定
  位 body 段直接读字节。
- `ResolveWrite(pc, idx)`：写参解析为 KV 路径。
- `NextPc(pc)`：纯函数，求后继 PC。

**规范要点**：需要整块字节（如 `[]uint8`）时**必须**走 `ResolveReadPath` + XValue body，**不得**用
`ResolveRead`（后者对数组只返首元素）。
	"#### -> /lib/kvlang/spec/runtime语义/notinmyrwircaps
}
