// 欢迎加入kvspace世界
lib kvlang/spec/runtime语义/cruntime与后端abi {
	r####"# C runtime 与后端 ABI

本章界定 runtime 的实现契约：统一的 `kvspace*` C ABI、C runtime 与 Rust runtime 的一致性要求、
durable 单句柄相干、后端装载。

## 小 runtime 大扩展

中央 runtime 只实现执行核心（取指-解码-执行、call/return/br/goto、native builtin 派发、`=` 拷贝、用
户函数调用）；tensor / LLM / agent / print / fs / http 全部交给扩展运行时（见 [[runtime开发规
范]]）。这条分工是**不可越界的职责线**：runtime-c 独占三件事，扩展一件都不许重做。

**runtime-c 拥有（唯一实现，扩展不得复刻）**：

1. **执行核心**：取指-解码-执行大循环、call/return/br/goto、vthread 调度、native builtin 派发、`=`
   拷贝语义。
2. **值语义与编解码**：headlenpow / langtype 的**唯一编解码器**在 kvspace 正典 codec（`kvspaceTlvEncode` /
   `kvspaceDecodeHead` / `kvspaceNewPtr`/`NewChar`/`NewBool`/`NewInt64`/`NewFloat64`）；runtime-c 的
   `xvalue.c` 只是其薄封装。langtype 串是类型的唯一真相，怎么拼、怎么解，只此一处。
3. **指令解码与签名校验**：`rwir_decode.c:RwirDecode`（稠密探查、遇空即停）、`/lib/<op>` 签名的读写码规范、
   `LangtypeValid`/`LangtypeMatch` 类型判定。

**扩展运行时套用核心，不复刻**：runtime-c 把上述三件能力经嵌入式 C ABI（下节 `kvlang_vthread.h` +
`kvlang_runtime.h`）对外开放。扩展运行时**嵌入**这套核心、复用其执行循环、native 派发与正典 codec，只
**自行解释己方 opcode**——不得另起执行核心、不得另写编解码器、不得复刻 native 算子。据此：

- **runtime-rs 是扩展运行时的参考实现**，不是第二套等价核心。它作为 myrwir 宿主，套用 runtime-c 核
  心，在自己的 **myrwircaps**（见 [[runtime开发规范]]）里登记 print/fs/http/networld/vthread 等「OS
  工具箱」rwir。其对 head/langtype 的读写**必须**走正典 codec，不得自持一份编解码。
- **未来的 deepx-gpu-compute（GPU 算子融合计算引擎）同架构**：同样嵌入 runtime-c 核心，只在其
  **myrwircaps** 里登记 tensor 融合算子——算子融合发生在扩展侧（见 [[runtime开发规范]]），核心的解释
  语义不变。

## C runtime 结构

`kvlang/runtime/` 是通用嵌入式执行器（纯 C + `extern "C"`），可嵌入 myrwir 项目（op-gpu、agent、
livebyte）。两组对外 C ABI：

- `kvlang_vthread.h`：核心执行器（`kvlangRuntimeConnect`/`Disconnect`/`Bootstrap`/`ExecuteVthread`/
  `KvspaceHandle` …）。
- `kvlang_runtime.h`：扩展 runtime 语义，**只暴露 kvspace 不提供的部分**。现有 9 个符号：`kvlangDefRwir`、
  `kvlangRwirextHandoff`、`kvlangRwirextNextPc`、`kvlangRwirextParams`、`kvlangRwirextResolveRead`、
  `kvlangRwirextResolveReadPath`、`kvlangRwirextResolveWrite`、`kvlangLangtypeValid`、
  `kvlangLangtypeMatch`。KV 存取（connect/get/write/del/list/tlv）**不在此**——扩展宿主自连 kvspace ABI，把
  句柄（`void *`）传进带句柄的 myrwir 函数。

其中 `LangtypeValid`/`LangtypeMatch` 与正典 codec 的 head 解码，正是 **def langtype 与 langtype** 对外的
两条 ABI（两者区别见 [[langtype签名类型表达式]]）：前者服务 **def langtype**（匹配侧——校验一条签名匹配
类型、把一个值的 `(kind,ndim,dims)` 匹配进它），后者服务 **langtype**（解析侧——把 head 里一条类型串解析
为 `(kind,ndim,dims,元素字节宽)`，事实源为 `kvspaceDecodeHead` 与 `kvlangKindexprParse`）。两者同吃一条
类型表达式串、共用唯一文法与 codec，差别只在**匹配** vs **解析**。

内部模块：`xvalue.c`（XValue 编解码薄封装）、`langtype.c` + `langtypetable.c`（langtype 校验/匹配与我
表格）、`kvspace.c`（kvspace ABI 封装）、`keytree.c`（路径构造）、`rwir_decode.c`（PC 解析 + Decode）、
`vthread.c`、`rwir*.c`（native 算子 + 静态注册表）、`kvcpu.c`（Execute 循环）、`runtime.c`（ABI
层）、`rwirext.c`（扩展宿主 ABI，含 `notinmyrwircaps`）、`logx.c`、`strbuf.c`、`const.h`。
myrwircaps 为**静态数组 + 线性查找**。

## 统一 `kvspace*` C ABI（去特化）

layout 与 runtime **都不对 kvspace 做特化**：不 dlopen、不双后端分支、不解析 scheme。二者只走一套
`kvspace*` 兼容 C ABI（无下划线 camelCase）：

```
kvspaceConnect / Close / Clear
kvspaceGet                       // 借用读：*out 指向后端常驻/回收空间，调用方不得 free
kvspaceSetValue                  // 验证并同步写入完整 XValue 与 ro/vid 元数据
kvspaceGetHead                   // 读 head 与独立 KV key 上的 ro/vid
kvspaceListLen / ListAt / Del / DelTree / Cp / CpTree
kvspaceMkindex / MkindexExt / RmindexExt / Watch
kvspaceTlvEncode / TlvEncodeMode / DecodeHead
kvspaceNewPtr / NewChar / NewBool / NewInt64 / NewFloat64
```

**读写模型**：

- **读**走 `kvspaceGet`——返回后端常驻内存的**借用偏移指针**，调用方只读、**不得** `free`。
- **写**走 `kvspaceSetValue`，在调用返回前完成整条 XValue 写入。worker 不能把尚未写完的 body
  保留在进程内，避免进程退出后 PC 已推进而变量仍为空。底层 `WriteInPlace` / `WriteNewPlace`
  为其他 ABI 调用方保留，runtime 不依赖其待填充缓冲。
- **前缀枚举**不一次性返回整段名单缓冲：`kvspaceListLen` 只返子项计数，`kvspaceListAt(idx)` 借用回收
  缓冲逐项取名——遍历即「先 ListLen 定计数、再逐 idx ListAt」。

**native 读参**：函数调用统一传地址；帧参数槽 `**T` 经实参键 `*T` 指向调用方 key。
`ResolveReadValue` 在执行当前指令时从 KVSpace 读取目标值。初始化 XValue 的指令可直接使用代码槽
中的内联字面量。读取后的临时解码值只在当前指令内有效，下一条指令重新读取 KVSpace。

**后端由链接决定**：构建期经环境变量选择链接哪个 kvspace 库（`kvspace-c` 或 `kvspace_durable`），两
者导出同一 ABI；runtime 的句柄结构只保留 `void *h`，代码零后端分支。

## 导出符号

符号清单的权威来源是**构建产物**——`nm -D --defined-only runtime/build/libkvlang_runtime.so | grep
'^kvlang'`。本文只约定各模块的语义与 ABI 分组，不抄清单（抄清单必漂移）：`kvlangRuntime*` 执行入口、
`kvlangKvcpu*` 取指-解码-执行循环、`kvlangCtl*` 控制流原语、`kvlangBuiltin*` native 算子（myrwircaps）、
`kvlangKv*` kvspace ABI 封装、`kvlangRwirext*`/`kvlangDefRwir` 扩展宿主 ABI、`kvlangXvalue*`/
`kvlangLangtype*` 值语义，以及 **def langtype（匹配）与 langtype（真实类型）**两组能力。
	"#### -> /lib/kvlang/spec/runtime语义/cruntime与后端abi
}
