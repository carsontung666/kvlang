// 欢迎加入kvspace世界
lib kvlang/spec/runtime语义/cruntime与后端abi_续 {
	r####"# C runtime 与后端 ABI（续）

## 三个后端

| 后端 | 库 | 存储 | 场景 |
|------|-----|------|------|
| shm | kvspace-c（C） | ART 树 + slotsboxmalloc + file-backed mmap | 高性能（训练/推理） |
| redis | kvspace-durable（Rust） | Redis | 基线验证 |
| fs | kvspace-durable（Rust） | 文件系统 | 本地持久化 |

`conn(dsn)` 按 scheme（`shm://` / `redis://` / `fs://`）选后端，DSN 是唯一的后端区分。三后端**语义必
须完全一致**：一致性判据是 `tutorial/` 全量三后端逐字节相同输出（见导言卷）。后端存储布局差异（如 fs
把 `·` 编码为 `·/` 嵌套目录）经对称编解码还原，**不是**语义差异。

## 核心与扩展宿主的一致性（runtime-c ↔ runtime-rs）

runtime-c 是唯一核心；runtime-rs 是套用该核心的扩展宿主（见 [[执行模型]]、[[cruntime与后端abi]]）。
因套用同一核心，二者的核心动态语义**天然一致**、不存在两套需人工对齐的核心；差异**仅限**须经 def
rwir 路由的 rwir 的**执行位置策略**，且对 kvlang 程序不可观测：

- **C runtime**：须经 def rwir 路由的 rwir（不在本 runtime myrwircaps 内的 rwir，见 [[runtime开发规
  范]]）一律经 `handoff_external_rwir`（Set `/lib/<op>/vids/<vtid>` + Watch 30s 超时）移交，供独立进
  程扩展消费；嵌入式模式（`KVMODE_RETURN`）经 `out_pc` 把控制交回同进程扩展。
- **Rust runtime**：`is_inproc(op)` 命中的 rwir 在驱动循环里就地 `dispatch`（无 handoff、无
  watch）；非就地 rwir 经 `kvlangRwirextHandoff` 移交独立进程扩展或委托宿主 `ext` 处理器。

二者对 native builtin、控制流、`=` 拷贝、值语义的处理**必须**一致；差异仅限须经 def rwir 路由的 rwir
的**就地 vs 移交**这一执行位置维度，且对 kvlang 程序不可观测（同一 tutorial 三后端两 runtime 输出一
致）。

## durable 单句柄相干

durable 后端（fs/redis）采用**惰性 flush**，只在**同一 kvspace 句柄内相干**。因此 C runtime 与其驱动
的扩展（Rust `run_vid` 循环、层内 rwir）**必须共用同一 kvspace 句柄**（`kvlangRuntimeKvspaceHandle`
取出后传给 myrwir）。若各持独立句柄，一方的写对另一方不可见，fs/redis 的 PC / 值会读到陈旧快照，导致
整段重跑或错乱。此为 normative 要求。

## 扩展宿主自连 kvspace

扩展宿主（op-gpu、numpy、Go json）**自己** `kvspaceConnect(dsn)` 拿句柄，rwir 移交只用
`kvlangRwirext*` 的 runtime 语义（`kvlangDefRwir`/`Params`/`ResolveRead`/`ResolveReadPath`/`ResolveWrite`
/`NextPc`）。tensor 类大 buffer **应**走 shm 后端零拷贝：`ResolveReadPath` 拿容器路径 → `kvspaceGet`
取 XValue 指针 → `DecodeHead` 定位 body → 在该地址原位建 ndarray，读写 ndarray 即读写 kvspace，无拷贝；
写走 `kvspaceTlvEncode` + `WriteInPlace`/`WriteNewPlace`。锚例：`tutorial/14-numpy/`（shm 全流程）、
`tutorial/14-networld/`（fs/proc）。
	"#### -> /lib/kvlang/spec/runtime语义/cruntime与后端abi_续
}
