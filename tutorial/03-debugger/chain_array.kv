# 深层数组参数链路：main → f1 → f2 → f3
# 在 f3 最深处放置 debugger() 调试标记；ART 控制器必须与执行器同进程
rwfunc f3() -> (a:int64, s:int64) {
    println(a)
    debugger()          # 调试标记；普通运行不暂停
    at(a, 0) -> v0
    at(a, 1) -> v1
    v0 + v1 -> s        # 30 + 40 = 70
}

rwfunc f2() -> (a:int64, s:int64) {
    a:int64 = [30, 40]  # f2 创建数组放写参
    f3() -> (a, s)      # 传给 f3 的写参，不拷贝，同路径直达
}

rwfunc f1() -> (s:int64) {
    f2() -> (_, s)      # 接收 s，丢弃数组
}

rwfunc init() -> () {
    f1() -> r
    println(r)            # 70
}
