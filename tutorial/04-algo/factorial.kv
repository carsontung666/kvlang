# factorial: 迭代阶乘
# 算法: while 循环累乘，计算 10! = 3628800
# 期望输出:
#   fact = 3628800
rwfunc factorial(n:int64) -> (result:int64) {
	1 -> result
	i <- 1
	while (i <= n) {
		result = result * i  # = 等价于 <-
		i + 1 -> i
	}
}

ans <- factorial(10)
println("fact =", ans)
