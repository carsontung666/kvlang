# gcd: 欧几里得最大公约数（尾递归）
# 算法: gcd(48, 18) = 6
# 期望输出:
#   gcd = 6
rwfunc gcd(A:int64, B:int64) -> (R:int64) {
	if (B == 0) {
		A -> R
	} else {
		rem <- A % B
		R = gcd(B, rem)  # = 等价于 <-
	}
}

gcd(48, 18) -> ans
println("gcd =", ans)
