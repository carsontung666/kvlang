// array_slice: 数组切片 [lo,hi)（原地语义：写槽即源）
// 规范依据: 02-kvspace模型/07-数组两种物理形态 —— array·slice 作用于 compact 时
//           先自动 scatter，即 compact 数组必须能直接 slice
// 语义: compact [1,2,3,4,5] 取 [1:3) 后应有 2 个元素（散列元素数用 kvspace·listlen 读）
// 期望输出:
//   2
rwfunc test() -> () {
	a:[]int64 = [1, 2, 3, 4, 5]
	array·slice(a, 1, 3) -> a
	kvspace·listlen(a) -> n
	println(n)
}
