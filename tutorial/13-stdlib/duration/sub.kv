// sub: 两 duration 相减（time/duration·sub 由 05-runtime语义/05-runtimec内建 权威列出）
// 语义: 5h - 3h = 2h。sub 与 add 是同族对称算子，实现须按 opcode 区分减法
// 期望输出:
//   2
rwfunc test() -> () {
	time/duration·hours(5) -> a
	time/duration·hours(3) -> b
	time/duration·sub(a, b) -> c
	time/duration·as_hours(c) -> n
	println(n)
}
